const initState = {
    query: '',
    loading: false,
    results: [],
    error: ''
}

function reducer(state = initState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer